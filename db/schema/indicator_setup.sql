  CREATE TABLE "INDICATOR_SETUP" 
   (	"INDICATOR_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY, 
	"INDICATOR_CODE" VARCHAR2(50) NOT NULL ENABLE, 
	"INDICATOR_DESC" VARCHAR2(255) NOT NULL ENABLE, 
	"INDICATOR_DATATYPE" VARCHAR2(20), 
	"CREATED_DT" DATE DEFAULT SYSDATE, 
	 CHECK (indicator_datatype IN ('NUMERIC','CHARACTER')) ENABLE, 
	 PRIMARY KEY ("INDICATOR_ID")
  USING INDEX  ENABLE, 
	 UNIQUE ("INDICATOR_CODE")
  USING INDEX  ENABLE
   ) ;